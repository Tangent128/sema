TODO:

==== alpha?

quit sema when in spawned mode & no scripts left

create api.proxy set of metatables
cmd() takes multiple args

usage help + README

==== beta?

don't killall script when main thread exits

script loading
resolve resources (dofile, chdir, etc) to script location
ensureFifo() / openRead() / openWrite()

prettyprint client output
limit server verbosity

check TODOs
evade DEBUG usage where possible
update Lua source version
test multiple copies of scripts (& different scripts) non-interfering
test packet concurrency (multiple packets in pipeline for sure, multiple reader threads if time?)
double check for error conditions/memory leaks/fd leaks

==== v0.9

env + "named" fds
"use"
wait for given time

==== v1.0

resolve file names to socket path (more realpathness + common prefix?)
socket activation?
writes should block coroutines on full pipe
inotify?
metrics?

